# Core dependencies
streamlit>=1.30.0
pandas>=2.0.0
pyreadstat>=1.2.0
requests>=2.31.0

# Data visualization (already included in streamlit, but explicit for clarity)
# plotly>=5.14.0  # Already in streamlit
# altair>=5.0.0   # Already in streamlit

# Optional - for Excel export (if needed)
openpyxl>=3.1.0

# For better performance with large datasets
pyarrow>=12.0.0

